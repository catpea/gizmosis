# core

Core helper functions shared by the prototype runtime and node-editor package.

This is intentionally small. The language is still XML-first; this folder only provides browser-oriented primitives needed by the live prototype.
