# Tools

The active compiler CLI lives in:

```text
src/compiler/cli.js
```

Common commands from the package root:

```bash
npm run check
npm run compile
npm run inspect
```

The compiler is intentionally dependency-free so it can run in constrained local-agent environments.
